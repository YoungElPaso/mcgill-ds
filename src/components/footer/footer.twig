{# 
  Internal Footer Component.
  Mainly contains smaller components to create an attractive Footer.
  Can be promoted to 'Shared' when appropriate.
  Has a required defaultClass value.
#}

{# Provide the required defaultClass value. #}
{%
  set _defaultClass = [
    'mds-footer',
  ]
%}

{# 
 TODO: footer probably should have optional logo component.
 Therefore hasLogo should be part of its props.
 
 Footer props should include: 
  hasLogo,
    - if hasLogo: LogoComponent (w/ assetPath and logoFile properties, width) 
  footerChildren (string?).
#}

{# TODO: 
   * specifically, later *commit*! 
   * and then resume Footer child flex styling on mobile etc,
   * then review w/ H and then port to WMS. #}

<!-- TODO: Crap, think need to use Grid rather than flexbox here... -->
<footer data-testid="mds-footer" class="{{_defaultClass}} text-center md:flex justify-center items-center p-4 px-8 bg-black text-white ">
{% block footerChildren %}
  {# Use include strategy for simpler components (no children/slots/embeds). #}
  {% include './footer-logo.twig' with {"modifiers":"inline-block mr-8"} %}
  
  {# Uses the block strategy of componentization! #}
  {% embed '../box/box.twig' %}
    {% block content %}
      <small class="text-gray text-sm"> {{'Copyright &copy; 2021 McGill University. All rights reserved.' | t }}</small>
    {% endblock %}
  {% endembed %}

  {% embed '../box/box.twig' with {"modifiers": "" }%}
    {% block content %}
      <a href="#">Accessibility</a>
    {% endblock %}
  {% endembed %}

  {% embed '../box/box.twig' with {"modifiers": " "} %}
    {% block content %}
      <a href="#">Privacy notice</a>
    {% endblock %}
  {% endembed %}

  {% embed '../box/box.twig' with {"modifiers": " "}%}
    {% block content %}
      <a href="#">Contact us</a>
    {% endblock %}
  {% endembed %}
{% endblock %}
</footer>

<br />
<!-- Legacy WMS Footer. -->
<!-- TODO: change this to use Grid so the logo can be left aligned and the links right aligned etc. -->
<div id="sub-footer-inner" class="text-center md:flex justify-center items-center p-4 px-8 bg-black text-white">
  <!-- Logo in the foam. -->
  <a id="mcgill-watermark" title="return to McGill University" href="https://www.mcgill.ca" class="inline-block">
    {% include './footer-logo.twig' with {"modifiers": 'mr-8'} %}
    <span id="university-watermark" class="offscreen sr-only">McGill University</span>
  </a>

  <!-- Copyright. -->
  <div id="copyright" class="m-3 p-3 text-gray text-sm">
    Copyright © 2021 McGill University
  </div>

  <!-- Admin Links. -->
  <div id="admin-links" class="m-3 p-3">
    <a class="m-3" id="accessibility-link" title="accessibility" href="https://www.mcgill.ca/accessibility">Accessibility</a>
    <a class="m-3" href="/desautels/saml_login?destination=node/65144" title="Site Maintenance">Sign in (site maintenance)</a>
  </div>
</div>
<!-- End of Legacy WMS Footer. -->