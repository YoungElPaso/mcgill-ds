{# Footer-Logo
  - Properties:
    - assetPath,
    - footerImage, 
    - modifiers.
#}

{# Provide McGill Logo as the default.#}
{% if footerImage is empty %}
  {% set footerImage = "mcgill-logo.svg" %}
{% endif %}
<img src="{{assetPath}}{{footerImage}}" width="144" class="{{modifiers}}"/>
